# ZitaoTech KEYPOINT 键盘使用说明书

> 本文档根据本仓库当前的固件配置（`config/zitaotech_keypoint.keymap`）编写，用于说明 KEYPOINT 分体蓝牙机械键盘的硬件结构、按键功能、各层（Layer）布局以及日常使用/维护方法。

---

## 一、硬件概览

KEYPOINT 是一把 **左右分体、双蓝牙（ZMK 固件）** 键盘，两侧各有：

| 部件 | 说明 |
|---|---|
| 主控 | Nordic nRF52840（每侧各一块） |
| 屏幕 | nice!view 低功耗显示屏（lpm009m360a，144×72，每侧一块） |
| 左侧指向设备 | **BB Trackpad 光学触摸板**（Avago A320 传感器） |
| 右侧指向设备 | **TrackPoint 小红点**（ThinkPad 风格指点杆） |
| 灯光 | 每侧 1 颗 WS2812 RGB 灯 + 自定义 LED（配置中标注 "unreal"，视硬件焊接情况而定） |
| 旋转编码器 | 左右各一个 EC11（配置中已启用，若硬件未安装则无功能） |
| 供电 | 每侧内置锂电池，通过 USB-C 充电（USB 或蓝牙均可输出） |

### 特殊物理键位（两半之间的中央区域）
在左右两半之间、触摸板/指点杆附近还有几个**小尺寸实体按键**：
- 中排 2 个小键（左右各 1 颗，75×75）→ 默认映射为 **鼠标左键单击**（`&mkp LCLK`）
- 下排 4 个小键（75×50）→ 默认映射为 **PgUp / PgDn / 向上滚动 / 向下滚动**

> 也就是说，这块键盘不只是打字，还内置了完整的"鼠标"（触摸板/小红点 + 实体按键 + 鼠标按键层）。

---

## 二、分层（Layer）总览

固件共定义了 5 层，层号与进入方式如下：

| 层号 | 名称 | 进入方式 |
|---|---|---|
| 0 | **QWERTY** | 默认层，开机即用 |
| 1 | **LOWER** | 按住任一侧的 `LOWER` 键（大拇指位置） |
| 2 | **RAISE** | 按住任一侧的 `RAISE` 键（大拇指位置） |
| 3 | **MOUSE** | ① LOWER 层中的 `MOUSE` 键切换进入；② 触摸/移动触摸板或小红点时**自动临时激活**（600ms） |
| 4 | **RESERVE** | 空层（全部无功能，预留） |

> 屏幕底部会显示当前激活层名（QWERTY / LOWER / RAISE / MOUSE…）。

### 分层机制小知识
- `LOWER`（层1）与 `RAISE`（层2）为**瞬时层**：按住时生效，松手即回到默认层。
- `MOUSE`（层3）为**切换层**：通过 LOWER 层的 `MOUSE` 键进入后保持激活；同时触摸板/小红点产生输入时也会临时启用 600ms，方便"手指不离开指点设备时直接用附近按键当鼠标键"。
- 某些键标为 `trans`（▽），表示"透传"，即沿用更底层（QWERTY 层）相同位置的功能。

---

## 三、各层键位详细说明

### 层 0：QWERTY（默认打字层）

```
   [Tap=Tab|Hold=Esc]   Q   W   E   R   T               │  Y   U   I   O   P   Backspace
   CapsLock   					A   S   D   F   G  [左|左键]     │  H   J   K   L   ;   Enter
   Shift                Z   X   C   V   B  PgDn PgUp    │  N   M   ,   .   /   Shift
   Ctrl                Win  Alt  Mute  Raise Spc Lower  │  Lower Spc  Raise Mute  -   =   `
```

| 位置 | 功能 | 说明 |
|---|---|---|
| 左上角小键 | **Tap = Tab / Hold = Esc** | `&mt ESC TAB` 复合键，单击出 Tab，长按出 Esc |
| 中央 2 个 75×75 小键 | **鼠标左键（单击）** | 方便食指点击 |
| 中央 4 个 75×50 小键 | **PgDn / PgUp / 向下滚 / 向上滚** | `SCRL_DOWN`/`SCRL_UP` 为鼠标滚轮动作 |
| 左拇指列 | Ctrl / Win / Alt / 静音 / **RAISE** / 空格 / **LOWER** | 手部内倾的人体工学拇指位 |
| 右拇指列 | **LOWER** / 空格 / **RAISE** / 静音 / `-` / `=` / `` ` `` | 与左侧镜像 |

**左右编码器（本层）**
- 左编码器：音量增 / 减（`C_VOL_UP` / `C_VOL_DN`）
- 右编码器：左 / 右方向键（`LEFT` / `RIGHT`）

---

### 层 1：LOWER（数字与符号层，按住 LOWER 生效）

```
   Esc   1   2   3   4   5   │  6   7   8   9   0   Delete
   Caps  ▽   ▽   ▽   ▽   ▽  [右击][右击]│  ▽   ▽   ▽   ▽   '   Enter
   Shift ▽   ▽   ▽   ▽   ▽   ▽   ▽  亮度- 亮度+  │  ▽   ▽   [   ]   \   Shift
   Ctrl  Win  Boot BT_clear ▽  Spc  Lower     │  Lower Spc MOUSE 输出切换 Boot ▽  ▽
```

| 位置 | 功能 | 说明 |
|---|---|---|
| 顶排 | **1–0 + Delete** | 数字键与删除 |
| 中央 2 个小键 | **鼠标右键（单击）** | |
| 中央 4 个小键 | 透传 / 透传 / **背光减 / 背光加** | `&bl BL_DEC` / `&bl BL_INC` 调节键盘背光 |
| 右侧符号 | `'` 引号、`[` `]` `\` | 上排 `[` `]` `\` 补全常用符号 |
| 左拇指列 | Ctrl / Win / **Bootloader** / **清除蓝牙配对** / ▽ / 空格 / LOWER | 详情见"刷机"与"蓝牙"章节 |
| 右拇指列 | LOWER / 空格 / **MOUSE（切入鼠标层）** / **USB⇄蓝牙切换** / **Bootloader** / ▽ / ▽ | `OUT_TOG` 切换输出方式 |

**编码器（本层）**
- 左编码器：切换蓝牙配置文件（下一个 / 上一个，`BT_NXT` / `BT_PRV`）
- 右编码器：上 / 下方向键

---

### 层 2：RAISE（功能键与方向键层，按住 RAISE 生效）

```
   Esc   F1   ↑   F3   F4   F5  │  F6   F7   F8   F9   F10   (无)
   Caps  ←   ↓   →   ▽   ▽   ▽  ▽  │  F11  F12  ▽   ▽   ▽   ▽
   Shift (其余为透传)              │  (其余为透传)
   Ctrl  Win  ▽   ▽  RAISE ▽  ▽  │  ▽  RAISE  ▽  ▽  ▽  ▽  ▽
```

| 位置 | 功能 | 说明 |
|---|---|---|
| 顶排 | **F1–F10** | 功能键 |
| 二排左端 | **← ↓ →** 方向键（`←`在左、`↓`在 D 下方、`→`在右） | |
| 二排右端 | **F11、F12** | |
| 左/右拇指 | 仍是 **RAISE** 键 | 可继续按住触发 |
| 其余 | 大多为空（`&none`） | 无功能 |

**编码器（本层）**：未单独定义 → 沿用默认层的功能（左=音量，右=左右方向）。

> 该层用途偏预留，方向键+F 键集中于此。

---

### 层 3：MOUSE（鼠标操作层）

> 进入方式：① LOWER 层按 `MOUSE` 键切换进入；② 手指触摸/移动**触摸板**或**小红点**时自动临时激活 600ms。
> 本层的核心思路：**右手 J/K/U/M 键 = 打字与鼠标键二合一**，多数键"单击=字母，长按=鼠标按键"。

```
   Tab  Q   W   E   R   T   │  Y   [U⇄中键]  I   O   P   Backspace
   Caps A   S   D   F   G  [左键][左键]│  H   [J⇄左键]  K   L   ;   Enter
   Shift Z   X   C   V   B  滚下  滚上  ▽   ▽  │  N   [M⇄右键]  ,   .   /   Shift
   Ctrl Win  Alt  ▽  RAISE Spc [左键]│ [左键单击] [右键/双击中键] RAISE Mute - = `
```

| 位置 | 功能 | 说明 |
|---|---|---|
| `U` 键 | **Tap=U / Hold=鼠标中键(MB3)** | `&M_kp MB3 U` |
| `J` 键 | **Tap=J / Hold=鼠标左键(MB1)** | `&M_kp MB1 J` |
| `M` 键 | **Tap=M / Hold=鼠标右键(MB2)** | `&M_kp MB2 M` |
| 中央 2 个小键 | 鼠标左键（单击） | |
| 左下小键区 | `滚下` / `滚上` | 保留滚轮 |
| 左拇指 | **鼠标左键（MB1）** | |
| 右拇指区 | **[左键单击]** `&LMouse_sc 0 MB1`（轻点=左键）<br>**[右键]** `&tdRMCLK`：单击=右键，双击=中键 | 双击=中键是 tap-dance 实现 |
| 空格两侧 | RAISE（瞬时）、其余字母保持可用 | 可边用鼠标边切层 |

**典型用法**：把手指放上触摸板移动光标，此时层 3 临时生效，J=左键、M=右键、U=中键，或直接按中央的实体鼠标键，即可完成整个鼠标操作而无需抬手。

> ⚠️ **注意**：本层没有"返回默认层"的 `&to 0` 键。若通过 LOWER→`MOUSE` 切换进入后，配置里**没有任何键能切回 QWERTY**（只能靠触摸板临时层释放后自动回落）。建议在 MOUSE 层大拇指处补一个 `&to 0` 键（见"如何修改"一节）。

---

### 层 4：RESERVE（预留空层）
所有键均为 `&none`，无任何功能，仅作后续自定义预留。

---

## 四、触摸板 / 小红点（指向设备）

| 设备 | 位置 | 行为 |
|---|---|---|
| **BB Trackpad 触摸板** | 左侧 | 手指滑动移动光标；轻触/按压产生左键点击事件；支持滚动 |
| **TrackPoint 小红点** | 右侧 | 施加压力移动光标（配置启用指数加速度 `CONFIG_TRACKPOINT_EXPONENTIAL`） |
| **触摸即进鼠标层** | 两侧 | 产生输入时自动临时激活 MOUSE 层 600ms，方便就近按键当鼠标键 |

> 触摸板侧（`left_bbtrackpad_keypoint.overlay`）还定义了滑动手势映射：触摸板的物理按键事件被转换为方向键（`INPUT_BTN_0→→`、`BTN_1→←`、`BTN_2→↓`、`BTN_3→↑`），可当作四向滑动输入。

---

## 五、屏幕（nice!view）显示说明

两块屏为 144×72 低功耗屏，显示内容分三块：

| 区域 | 内容 |
|---|---|
| 顶部 | 电池图标 + 电量百分比；连接状态图标（USB 符号 / 蓝牙已连 / 蓝牙未连 / 未配对）；WPM 打字速度曲线 |
| 中间 | **4 个圆形蓝牙配置指示**，当前选中配置高亮（数字 1–4） |
| 底部 | 当前**层名**（QWERTY / LOWER / RAISE / MOUSE / RESERVE） |

右侧屏幕还额外会**每 60 秒随机切换一张萌系图片**（猫、宇航员等，见 `lpm_view/widgets`）。

---

## 六、蓝牙连接与切换

ZMK 支持**最多 5 个蓝牙配置文件**，可用于多台设备（手机、笔记本、平板等）。

| 操作 | 按键组合 |
|---|---|
| 进入配对模式 | 选择某个蓝牙配置后，在**被配对设备**的蓝牙列表里选择 `KEYPOINT` |
| 切换蓝牙配置 | LOWER 层旋转**左编码器**（上一个/下一个）；或用屏幕中间的高亮圆点判断当前配置 |
| 清除当前配置的配对 | LOWER 层按 **BT_Clear** 键（左拇指区） |
| 清除所有配对（恢复出厂） | 刷 `settings_reset` 固件（见下文） |
| 切换输出方式 USB⇄蓝牙 | LOWER 层按 **输出切换** 键（`OUT_TOG`，右拇指区） |

> 提示：左右两半之间通过私有 BLE 连接（一侧为主、一侧为从），主侧负责与电脑通信。屏幕顶部的连接图标用于判断主侧与电脑的连接状态。

---

## 七、充电与电量

- 每侧各有一块电池，通过各自的 USB-C 口充电。
- 屏幕顶部显示本侧电量百分比；插上 USB 时图标会指示充电状态。
- 电量测量为电压分压方案（`zmk,battery-voltage-divider`），电量显示会随电压变化。

---

## 八、刷机与固件更新

### 8.1 从 GitHub 编译（无需本地环境）
1. 修改 `config/zitaotech_keypoint.keymap`（或使用 [keymap-editor 网页版](https://nickcoutsos.github.io/keymap-editor/)）后提交。
2. 触发 GitHub Actions（push 或手动 `workflow_dispatch`），等待编译完成。
3. 从 Actions 产物下载 `firmware.zip`，解压得到左右两个固件：
   - 左：`lpm_view;left_bbtrackpad_keypoint-zitaotech_keypoint_left-zmk.uf2`
   - 右：`lpm_view;right_trackpoint_keypoint-zitaotech_keypoint_right-zmk.uf2`
4. 对应刷到对应侧。

> `build.yaml` 已定义两套构建：左半 `left_bbtrackpad_keypoint`、右半 `right_trackpoint_keypoint`，各含 `lpm_view` 屏幕。

### 8.2 进入刷机模式（Bootloader）
- 方法一：按住 **LOWER 层的 `Bootloader` 键**（左半：左拇指区第 3 个键；右半：右拇指区 `输出切换` 右侧的键）。
- 方法二：部分版本也可通过按住键盘上的复位/启动按键上电进入。

进入后该半侧会以 U 盘（UF2 Bootloader）形式出现在电脑上。

### 8.3 写入固件
把对应侧的 `.uf2` 文件**拖入**该侧的 U 盘，设备会自动重启完成写入。

### 8.4 恢复出厂（清除全部设置与配对）
修改 `build.yaml`，把注释掉的 `settings_reset` 屏蔽组合加回，例如：
```yaml
include:
  - board: zitaotech_keypoint_left
    shield: lpm_view;left_bbtrackpad_keypoint;settings_reset
  - board: zitaotech_keypoint_right
    shield: lpm_view;right_trackpoint_keypoint;settings_reset
```
编译刷入后，键盘会清除所有蓝牙配对与设置并自动恢复默认。刷完请改回原 `build.yaml`。

---

## 九、如何修改键位

1. **编辑 `config/zitaotech_keypoint.keymap`**：这是核心配置文件，采用 ZMK 的 Devicetree 语法，每个 `layer` 块对应一层。
2. **可视化编辑**：访问 [keymap-editor 网页](https://nickcoutsos.github.io/keymap-editor/)，登录 GitHub 后选择本仓库即可拖拽编辑，保存后自动触发编译。
3. 常用改动示例：
   - 换键：把某键的 `&kp X` 改成 `&kp Y`。
   - 补 MOUSE 层出口：在 MOUSE 层拇指处加 `&to 0`。
   - 启用行内 HomeRow 修饰键：配置里已定义 `hm`（hold-tap，tapping-term 500ms）但**当前未被使用**，可把默认层的字母键改成 `&hm LSHFT A`、`&hm LALT S` 这类。
4. 编辑后提交 → Actions 编译 → 下载刷入即可。

---

## 十、常见问题（FAQ）

- **打字时突然进入鼠标模式？** 手指滑到触摸板会临时激活 MOUSE 层 600ms，属正常设计；若误触，手指离开触摸板 600ms 后自动恢复。
- **切到 MOUSE 层后回不去？** 当前固件该层没有返回键，建议添加 `&to 0`（见上）。
- **蓝牙连不上？** 先确认当前配置文件（屏幕中间圆点），用 LOWER+左编码器切换；仍不行则按 `BT_Clear` 后重新配对，或刷 `settings_reset`。
- **某侧按键没反应？** 检查该侧电量与主从连接；也可重刷该侧固件。
- **背光/灯光不亮？** 配置中背光（`&bl`）与 WS2812 均标为 "unreal"，可能硬件未焊接，可忽略。

---

*本说明书基于仓库当前版本固件编写，修改键位后请以实际固件行为为准。*
